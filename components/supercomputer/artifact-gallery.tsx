import type { ExecutionArtifact } from "@/services/orchestrator/orchestrator.types";
export function ArtifactGallery({artifacts}:{artifacts:ExecutionArtifact[]}){return <section className="rounded-3xl border border-white/10 bg-white/[.025] p-5"><h2 className="font-semibold">Artefatos</h2>{artifacts.length===0?<p className="mt-3 text-sm text-white/40">Nenhum artefato produzido ainda.</p>:<div className="mt-4 grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{artifacts.map((artifact)=><a key={artifact.id} href={artifact.url||artifact.path} download className="rounded-2xl border border-white/10 bg-black/20 p-4 hover:border-[#9D7CFF]/50"><span className="block text-[10px] uppercase tracking-wide text-[#9D7CFF]">{artifact.type}</span><span className="mt-1 block truncate text-sm">{artifact.name}</span></a>)}</div>}</section>}
