# MuseTalk model assets

Place MuseTalk checkpoints and supporting model assets here when the service owns the model files locally.

In production you can also point `MUSETALK_REPO_PATH` at an existing MuseTalk checkout and keep checkpoints in the structure expected by that repository.
